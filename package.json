{
  "name": "expense-tracker-functions",
  "version": "1.0.0",
  "description": "expense-tracker-functions",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "functions-framework --target=sendMail --source=functions/sendMail --port=8080",
    "dev": "functions-framework --target=sendMail --source=functions/sendMail --port=8080",
    "deploy:sendMail": "gcloud functions deploy sendMail --runtime nodejs20 --trigger-http --allow-unauthenticated --source=functions/sendMail --entry-point=sendMail --env-vars-file=.env.yaml"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devshah17/expense-tracker-functions.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/devshah17/expense-tracker-functions/issues"
  },
  "homepage": "https://github.com/devshah17/expense-tracker-functions#readme",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "nodemailer": "^7.0.13"
  },
  "devDependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "nodemon": "^3.1.11"
  }
}
